# Production uses Render's static-site rewrite routes (render.yaml) to proxy
# API requests to the backend services, keeping everything same-origin.
# Leave service URLs empty so requests use relative paths (/api/products, /api/orders).
VITE_PRODUCT_SERVICE_URL=
VITE_ORDER_SERVICE_URL=
VITE_API_KEY=changeme-dev-only
